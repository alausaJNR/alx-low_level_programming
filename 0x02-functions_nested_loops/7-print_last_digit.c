#include "main.h"

/**
 * print_last_digit-prints the last digit of an interger.
 * @a:number to compute last digit.
 * Return:last digit.
 */
int print_last_digit(int a)
{
	int last_digit, p;

	last_digit = a %  10;
	if (last_digit < 0)
	{
		_putchar(p = last_digit * -1)
	}
	else
		return (last_digit);
}


