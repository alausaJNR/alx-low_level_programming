#include "main.h"
/**
 * _isdigit - it checks for an input being number
 * @c: inputs
 * Return: 1 if c is a digit and 0 if otherwise
 */
int _isdigit(int c)
{
	if ((c >= 0) && (c <= 9))
		return (1);
	else
		return (0);
}
			
